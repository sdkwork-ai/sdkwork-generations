# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.cloud.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/generations/webserver.pid;
error_log /var/log/sdkwork/generations/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    include snippets/adaptive-web.maps.conf;
    upstream gateway {
        least_conn;
        keepalive 32;
        server sdkwork-api-cloud-gateway:8080;
    }
    server {
        listen 80;
        server_name generations-test.sdkwork.com generations-test.birdcoder.com generations-test.dtupay.com generations-test.sdkwork.cn generations-test.birdcoder.cn generations-test.dtupay.cn generations-test.skubc.com generations-test.skubc.cn generations-test.zowalk.com generations-test.zowalk.cn generations-test.offer86.com generations-test.offer86.cn generations-test.86offer.com generations-test.86offer.cn;
        include snippets/gateway-locations.nonproduction.conf;
    }
}
