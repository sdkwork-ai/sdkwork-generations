# sdkwork-generations-mcp-service

Model Context Protocol adapter exposing generation capabilities to chat
agents as MCP tools (`generation.image.create`, `generation.video.create`,
`generation.speech.create`, `generation.music.create` plus retrieve tools),
backed by the generations service and provider adapters.
